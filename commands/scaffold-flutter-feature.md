---
name: scaffold-flutter-feature
description: Generate a new Flutter feature skeleton with clean architecture and selected state-management pattern.
---

Create a new feature module scaffold.

1. Follow `../skills/scaffold-flutter-architecture/SKILL.md`.
2. Enforce `../rules/flutter-development-best-practices.mdc`.
3. Enforce `../rules/dart-effective-dart.mdc`.
4. Build minimal complete skeleton first (UI + state + data boundaries).
5. Add starter tests with `write-widget-test` / `write-bloc-test`.
